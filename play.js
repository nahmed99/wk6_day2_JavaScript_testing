const assert = require("assert");

// equal uses == operator, and is now deprecated.
// strictEqual uses === operator, and is the on eto use...
assert.strictEqual("2", 2);     //(actual_result, expected_result) - different to assert in Python.