const assert = require('assert');
const Taxi = require('../taxi.js'); // Don't need to use the .js in the file name?

// function from mocha suite: describe(name_of_tests, function - inline in the case below)
describe('Taxi', function () {




})